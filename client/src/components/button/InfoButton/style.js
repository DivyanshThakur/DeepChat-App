import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  root: {
    height: "0.8125rem",
    width: "0.8125rem",
    borderRadius: "50%",
    marginLeft: "0.5rem",
  },
}));

export default useStyles;
